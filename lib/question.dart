class Question {
  String questionText;
  bool questionAnswer = true;

  Question({this.questionText = 'Oi', this.questionAnswer = true}){
    questionText = questionText;
    questionAnswer = questionAnswer;
  }
}